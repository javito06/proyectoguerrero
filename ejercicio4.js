let numero1=prompt("digite un numero:")
let numero2=prompt("digite un numero:")
let multiplicacion= parseInt(numero1) * parseInt(numero2)
if(numero2===0){
    alert("no se puede dividir")
}else{
    let division= parseInt(numero1)/parseInt(numero2)
    console.log("la division no es posible realizar");
}